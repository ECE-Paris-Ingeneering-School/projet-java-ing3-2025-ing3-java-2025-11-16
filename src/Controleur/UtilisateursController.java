package Controleur;

import Modele.*;
import Dao.*;
import java.util.List;

public class UtilisateursController {

/*
    connexion(String email, String motDePasse) : boolean → Vérifie les identifiants et retourne si la connexion est valide.

    deconnexion() → Déconnecte l'utilisateur actuel.

    inscription(Utilisateur utilisateur) : boolean → Ajoute un nouvel utilisateur.

    modifierUtilisateur(Utilisateur utilisateur) : boolean → Met à jour les informations d'un utilisateur.

    supprimerUtilisateur(int idUtilisateur) : boolean → Supprime un utilisateur par son ID.

    getUtilisateur(int idUtilisateur) : Utilisateur → Récupère les infos d’un utilisateur spécifique.
*/

}
